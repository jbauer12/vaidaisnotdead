{{!< default}}

<div class="container">
    <div class="tag-header">
        <p class="section-kicker">Rechtliches</p>
        <h1 class="tag-title">Impressum</h1>
    </div>

    <div class="page-content">
        {{#get "pages" filter="slug:impressum" limit="1"}}
        {{#foreach pages}}
        {{content}}
        {{/foreach}}
        {{/get}}
    </div>
</div>
